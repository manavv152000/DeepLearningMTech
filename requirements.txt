# Deep Learning Dependencies
torch>=2.0.0
torchvision>=0.15.0

# Image Processing
Pillow>=9.0.0

# Scientific Computing
numpy>=1.21.0

# Optional: For faster data loading (if needed)
# Note: num_workers is set to 0 in the code, so this is optional
# torch-audio>=2.0.0

# Optional: For GPU support (uncomment if using CUDA)
# torch-cuda>=2.0.0

# Optional: For Jupyter notebooks (if needed for development)
# jupyter>=1.0.0
# matplotlib>=3.5.0
# seaborn>=0.11.0